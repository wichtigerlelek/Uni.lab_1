#include "utils.h"
#include <iostream>
#include <algorithm>
#include <stdexcept>
#include <unordered_set>


DiGraph get_graph_from_adjacency_matrix(const Matrix<bool>& adjacency_matrix){ return DiGraph(); }

Matrix<bool> get_reachability_matrix(const DiGraph& graph){ return Matrix<bool>(1, 1); }

